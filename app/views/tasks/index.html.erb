<p style="color: green"><%= notice %></p>

<% content_for :title, ApplicationHelper::BRAND_NAME + " | Tasks" %>

<h1>Tasks</h1>

<div id="tasks">
<% @tasks.each do |task| %>
  <%= render task %>
  <p>
  <p>Skapad av: <%= task.user.email %></p>
  <%= link_to "Show this task", task %>
    </p>
  <% end %>
</div>

<%= link_to "New task", new_task_path %>

